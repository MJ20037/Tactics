using UnityEngine;

public class GameManager : MonoBehaviour
{
    public bool isPlayerTurn=true;//Check whose turn currnetly is
    public GameObject selectedPlayer=null;//Player unit currently selected
}